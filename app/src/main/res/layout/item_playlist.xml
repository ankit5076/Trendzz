<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/parent_item_playlist"
    >
    <de.hdodenhof.circleimageview.CircleImageView
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/playlist_image"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_marginStart="@dimen/view_default_margin"
        android:layout_marginTop="@dimen/view_default_margin"
        tools:src="@drawable/fitmuscletv"
        android:layout_marginBottom="@dimen/view_default_margin"
        app:civ_border_width="1dp"
        app:civ_border_color="#FF000000"/>

    <TextView
        android:id="@+id/playlist_channel"
        android:layout_width="match_parent"
        android:gravity="center"
        android:textColor="@color/primaryText"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/playlist_image"
        android:layout_alignTop="@id/playlist_image"
        android:layout_alignBottom="@id/playlist_image"
        tools:text="@string/channel_name"
        android:textSize="20sp" />

</RelativeLayout>