<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.trends.trending">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<activity android:name=".DummyBottom">-->
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MAIN" />-->

                <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        <!--</activity>-->
        <activity android:name=".ui.FamousQuote"
            android:theme="@style/MyMaterialTheme"/>
        <activity android:name=".ui.Video"
            android:theme="@style/MyMaterialTheme"/>
        <activity android:name=".DummyBottom"/>
        <activity android:name=".DummyUploadQuote" />
        <activity android:name=".ui.PlaylistVideo" />
        <activity android:name=".ui.UserUploadQuote" />

        <service android:name="com.trends.trending.repository.VideoRepository" />
        <service android:name=".service.DownloadService" />
    </application>

</manifest>